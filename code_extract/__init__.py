"""Bóc tách feature bằng CODE thay vì gọi LLM từng trang.

`common.py`/`runner.py`/`lexicon.py` viết tay và ổn định; `rules.py` + `sites/*`
do agent sinh ra một lần từ cấu trúc HTML đã crawl (xem pipeline/codegen.py).
"""
